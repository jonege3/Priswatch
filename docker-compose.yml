services:
  priswatch:
    build: .
    container_name: priswatch
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - ./data:/data
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DB_PATH=/data/prisvakt.db
      - TZ=Europe/Oslo
